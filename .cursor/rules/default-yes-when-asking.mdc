---
description: 用户每遇到任何需确认或选择的场景时，自动优先采纳推荐/肯定选项，省略询问，立即推进下一步。
alwaysApply: true
---

# 总是自动采纳推荐项，无需征询，实时续推进

凡涉及选择、确认、批准等决策，无论是 commit/push、PR、修复等操作，均自动默认用户同意推荐（或肯定）选项，直接执行后续步骤，无需额外询问，确保决策顺畅高效。

**例外（不可默认同意）：** vole / vole-macos 的**发版**（Release、版本 tag、分发渠道发布、正式版本号发版流程等）必须先询问并得到用户明确确认，见 `release-requires-confirmation.mdc`。该例外优先于本规则。

规则如下：

1. **无需征询，即视为同意。** 遇到“需要/是否”时，无需确认，直接按建议执行（发版除外）。
2. **确认总是自动通过。** 所有询问类确认自动肯定，直接下一步（发版除外）。
3. **操作时简明说明，立刻执行。** 给出简要进度提示，即刻推动任务。
4. **严守安全边界。** 禁止强制推送主分支、泄露敏感信息、执行未经授权的危险操作。
5. **针对不可逆或范围不明确的行为，仅进行最小必要的“是”，不做过度处理。**

始终严格遵循：有多种选择时，自动接纳最优/推荐项，不让用户反复确认，自动流畅完成任务。

## 行为模式总结（源自长期历史会话规律）

1. **确认性问题从未被否定，始终采纳“需要/同意”等正向答复。** 用户明确指示所有“需不需要”问题一律自动视为“需要”，不重复确认。
2. **多项选择总采用推荐项或编号字母（如“1”“b”等）回复。** 遇到“你推荐哪一个”，用户总是采纳推荐回答→无需等待再选，自动采用建议推进。
3. **任务流连续自动衔接。** 用户一贯要求“执行下一步”“继续所有剩余 task”，意即完成一步自动进入下一步，直到全部完成或真被阻塞才中止。
4. **每完成一个 task 即自动 commit。** 用户已多次明确要求任务级别粒度提交，无需反复确认。
5. **避免无响应卡顿，主动汇报进度。** 如遇长操作或后台处理，需主动简述进度，防止用户因状态不明而疑惑。
6. **全程中文交互。** 包括询问、提示、总结，始终默认中文，契合用户期望。
7. **合并 main 前须 code review 并 CI 通过。** 见 `code-review-before-merge.mdc`：未 review 不得合并；「太简单 / CI 已绿」不能跳过。
8. **默认为 inline 工作流。** Subagent-Driven 与 inline 选择时，总是优先 inline。

此规则确保体验流畅、高效、少干扰且符合用户长期习惯。