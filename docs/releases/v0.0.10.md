# v0.0.10 — Guard cmdline + FCP generated（474 规则）

**日期**：2026-07-30  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：**Developer ID Application: Kong Wu (WCYC8XY4V2)**  
**公证**：Apple Notary Service（Accepted；CLI 无 staple，首次运行联网验证）

## 亮点

- **474** 条清理规则（较 v0.0.9 的 470：云盘 guard + FCP generated 等）
- `not_running` 精确进程名 + **`not_running_cmdline`（`pgrep -f`）**
- **Final Cut Pro generated cache**：仅 `~/Movies/*.fcpbundle` 内可再生成 Render/Proxy 媒体
- Dropbox / Google Drive / OneDrive cache 带进程守卫
- inventory 按路径对齐；Developer ID TCC 矩阵文档化

## 安装

```bash
curl -LO https://github.com/wukongnotnull/vole/releases/download/v0.0.10/vole-0.0.10-aarch64-apple-darwin.tar.gz
tar xzf vole-0.0.10-aarch64-apple-darwin.tar.gz
install -m 755 vole-0.0.10-aarch64-apple-darwin/bin/vole ~/.local/bin/vole
mkdir -p ~/.local/share/vole && cp -R vole-0.0.10-aarch64-apple-darwin/share/vole/rules ~/.local/share/vole/
export VOLE_RULES_DIR="$HOME/.local/share/vole/rules"
```

Homebrew：

```bash
brew tap wukongnotnull/vole https://github.com/wukongnotnull/vole
brew update
brew upgrade vole
```

## 自 v0.0.9 以来

| 项 | 说明 |
|---|---|
| Guard | cmdline `pgrep -f` + 云盘 / Firefox 守卫 |
| FCP | generated cache custom handler |
| 规则 | 470 → **474** |
| 工程 | inventory path 匹配；TCC DevID 矩阵 findings |
