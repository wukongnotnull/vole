# v0.0.11 — v1 CLI 收口（509 规则）

**日期**：2026-07-30  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：**Developer ID Application: Kong Wu (WCYC8XY4V2)**  
**公证**：Apple Notary Service（Accepted；CLI 无 staple，首次运行联网验证）

## 亮点

- **509** 条清理规则（较 v0.0.10：浏览器/UTM guard、Adobe/Pacifist/Recent、剪映 generated、Mail/云盘具名、XCTestDevices）
- **宣告 v1 CLI 产品目标完成**（`status` / `analyze` / `clean` / `history` + 协议冻结 + 签名分发）
- 进程守卫：精确名 + cmdline；FCP / 剪映 generated custom handler

## 安装

```bash
curl -LO https://github.com/wukongnotnull/vole/releases/download/v0.0.11/vole-0.0.11-aarch64-apple-darwin.tar.gz
tar xzf vole-0.0.11-aarch64-apple-darwin.tar.gz
install -m 755 vole-0.0.11-aarch64-apple-darwin/bin/vole ~/.local/bin/vole
mkdir -p ~/.local/share/vole && cp -R vole-0.0.11-aarch64-apple-darwin/share/vole/rules ~/.local/share/vole/
export VOLE_RULES_DIR="$HOME/.local/share/vole/rules"
```

Homebrew：

```bash
brew tap wukongnotnull/vole https://github.com/wukongnotnull/vole
brew update
brew upgrade vole
```

## 自 v0.0.10 以来

| 项 | 说明 |
|---|---|
| PR #19 | 浏览器 App Support guards + UTM；Adobe/Pacifist/Recent；JianyingPro generated |
| PR #20 | Mail/云盘具名缓存；XCTestDevices 复合 guard |
| 规则 | 474 → **509** |
| 文档 | v1 closeout 范围冻结 |
