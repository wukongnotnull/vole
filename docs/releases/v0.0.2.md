# v0.0.2 — Phase 4c+ Batch 6–8

**日期**：2026-07-30  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：ad-hoc（无 Developer ID）

## 亮点

- **`vole clean`** — **270** 条 macOS 清理规则（v0.0.1 为 150）
- **Batch 6–8**：流媒体/游戏/AI 桌面/DB 客户端/Steam/Claude/Qoder 等
- **保护层 refine**：`Library/Caches` 下 explicit 规则不再被 bundle guard 误 skip
- 其余同 v0.0.1（plan/apply、coverage_note、Mole 兼容 JSON）

## 安装

与 v0.0.1 相同，将 URL 中的 `v0.0.1` 换为 **`v0.0.2`**：

```bash
curl -LO https://github.com/wukongnotnull/vole/releases/download/v0.0.2/vole-0.0.2-aarch64-apple-darwin.tar.gz
tar xzf vole-0.0.2-aarch64-apple-darwin.tar.gz
install -m 755 vole-0.0.2-aarch64-apple-darwin/bin/vole ~/.local/bin/vole
mkdir -p ~/.local/share/vole && cp -R vole-0.0.2-aarch64-apple-darwin/share/vole/rules ~/.local/share/vole/
export VOLE_RULES_DIR="$HOME/.local/share/vole/rules"
```

## 自 v0.0.1 以来

| 版本 | 规则数 | 主要变化 |
|---|---|---|
| v0.0.1 | 150 | Phase 4c v1 首版 |
| v0.0.2 | **270** | Batch 6 (+40) + Batch 7 (+40) + Batch 8 (+40) + 保护层 refine |

## 已知限制

同 v0.0.1（无 notarization、TCC deferred）。
