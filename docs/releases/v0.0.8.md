# v0.0.8 — Developer ID 签名首版

**日期**：2026-07-30  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：**Developer ID Application: Kong Wu (WCYC8XY4V2)**

## 亮点

- 首个 **Developer ID** 签名的 release tarball（470 规则）
- 同 v0.0.7 规则集；安装体验改善（Gatekeeper 友好）

## 安装

```bash
curl -LO https://github.com/wukongnotnull/vole/releases/download/v0.0.8/vole-0.0.8-aarch64-apple-darwin.tar.gz
tar xzf vole-0.0.8-aarch64-apple-darwin.tar.gz
install -m 755 vole-0.0.8-aarch64-apple-darwin/bin/vole ~/.local/bin/vole
mkdir -p ~/.local/share/vole && cp -R vole-0.0.8-aarch64-apple-darwin/share/vole/rules ~/.local/share/vole/
export VOLE_RULES_DIR="$HOME/.local/share/vole/rules"
```

Homebrew：

```bash
brew tap wukongnotnull/vole https://github.com/wukongnotnull/vole
brew install vole
```

## 自 v0.0.7 以来

| 版本 | 签名 |
|---|---|
| v0.0.7 | ad-hoc |
| v0.0.8 | **Developer ID** |

Notarization（公证）待配置 `VOLE_NOTARY_PROFILE` 后下一版启用。
