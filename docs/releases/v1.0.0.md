# v1.0.0 — SemVer 对齐（511 规则）

**日期**：2026-07-30  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：**Developer ID Application: Kong Wu (WCYC8XY4V2)**  
**公证**：Apple Notary Service（Accepted；CLI 无 staple，首次运行联网验证）

## 亮点

- **包版本对齐 1.0.0**：`Cargo.toml` / `vole --version` / GitHub Release / Formula / README 一致（见 [SemVer 策略](../wukong-code/specs/2026-07-30-semver-policy-design.md)）
- **511** 条清理规则（含 v0.0.11 收口 + plan 去重 / Codex staging / JetBrains Toolbox keep-N）
- **v1 CLI 范围冻结**：`status` / `analyze` / `clean` / `history` + 协议冻结 + 签名分发

## 安装

```bash
curl -LO https://github.com/wukongnotnull/vole/releases/download/v1.0.0/vole-1.0.0-aarch64-apple-darwin.tar.gz
tar xzf vole-1.0.0-aarch64-apple-darwin.tar.gz
install -m 755 vole-1.0.0-aarch64-apple-darwin/bin/vole ~/.local/bin/vole
mkdir -p ~/.local/share/vole && cp -R vole-1.0.0-aarch64-apple-darwin/share/vole/rules ~/.local/share/vole/
export VOLE_RULES_DIR="$HOME/.local/share/vole/rules"
```

Homebrew：

```bash
brew tap wukongnotnull/vole https://github.com/wukongnotnull/vole
brew update
brew upgrade vole
```

## 自 v0.0.11 以来

| 项 | 说明 |
|---|---|
| SemVer | workspace `0.0.1` → **`1.0.0`**；禁止再发 `0.0.12` |
| PR #21 | plan 同路径去重；Codex Desktop stale staging；JetBrains Toolbox keep-N |
| 规则 | 509 → **511** |
| 文档 | SemVer 策略生效；v1 closeout 产品目标与包版本对齐 |
