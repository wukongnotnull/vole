# v1.21.0 — private-tmp (≥7 days, depth 1)

## Highlights

- 新增规则 `private-tmp`：清理 `/private/tmp` 与 `/private/var/tmp` 下直接文件叶（深度 1、≥7 天），`sudo -n` 永久删除
- 形状谓词 + apply 专用 carve-out（不依赖三树 allowlist）
- 故意严于 Mole 删除 maxdepth 5，对齐其 probe maxdepth 1

## Notes

- 规则数 **527**；零协议 bump
