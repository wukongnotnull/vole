# v1.22.0 — library-caches-temp (≥7 days)

## Highlights

- 新增规则 `library-caches-temp`：清理 `/Library/Caches` 下深度 ≤5 的 `*.cache` / `*.tmp` / `*.log`（≥7 天），`sudo -n` 永久删除
- 对齐 Mole 全树扫描（含 `com.apple.*`）；形状谓词 + apply 专用 carve-out

## Notes

- 规则数 **528**；零协议 bump
