# v1.24.0 — code-sign-clone

## Highlights

- 新增规则 `code-sign-clone`：清理 `/private/var/folders/**/X/**/*.code_sign_clone` 目录（深度 ≤5），`sudo -n` 永久删除
- 对齐 Mole；显式跳过 endpoint-security / EDR 路径

## Notes

- 规则数 **530**；零协议 bump
