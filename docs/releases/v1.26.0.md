# v1.26.0

## 变更

- CLI 特权 `clean --apply`：TTY 下 `probe` 失败时可至多一次 `sudo -v` 缓存管理员凭证，随后仍用 `sudo -n` 删除
- 非 TTY / CI / `VOLE_TEST_NO_AUTH=1` 行为与 1.25.0 一致（不弹密码）
- 桌面 SMAppService / `Install macOS*.app` 仍未移植

## 非目标

- 交互 `sudo rm`、图形密码框、Touch ID
- plan 阶段任何 sudo
