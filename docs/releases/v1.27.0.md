# v1.27.0

## 新增

- `install-macos-apps`：清理过期的 `/Applications/Install macOS*.app`
  - Software Update `RecommendedUpdates` **fail-closed**（仅显式 `[]` 才清理）
  - 当前 macOS 大版本 keep、运行中跳过、age ≥ 14 天
  - apply：特权 + 永久 `sudo -n`（TTY 可先 `sudo -v`）

## 明确不做

- 不删除 `/Library/Updates`、`/macOS Install Data`
- 桌面 SMAppService / 特权助手仍未移植

## 规则

531 → **532**
