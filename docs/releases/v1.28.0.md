# v1.28.0

## 新增

- 规则 `tm-failed-backups`：清理陈旧 Time Machine `*.inProgress` / `*.inprogress`
  - 门控：tmutil、AutoBackup、destination、非 Running/Unknown、本地卷、跳过网络 FS
  - 年龄 ≥ 48h；HFS `Backups.backupdb` + 已挂载 backupbundle/sparsebundle
  - apply：`tmutil delete`（非 sudo rm）

## 仍未移植

- 本地快照报告、桌面 SMAppService / 特权助手

## 规则

532 → **533**
