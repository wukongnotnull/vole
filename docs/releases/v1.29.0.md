# v1.29.0

## 新增

- 本地快照报告：`tmutil listlocalsnapshots /` → 数量 + review 提示
  - 挂载：`vole status`（TUI / JSON `local_snapshots`）；可选 `analyze` 提示行
  - 仅报告；不删除本地快照（禁止 `deletelocalsnapshots` / clean apply）
  - fail-closed：`tmutil` 失败或超时不报假数量

## 仍未移植

- 桌面 SMAppService / 特权助手

## 规则

533（不变）
