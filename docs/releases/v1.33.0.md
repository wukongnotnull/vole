# v1.33.0

## 新增

- **`vole uninstall` Homebrew Cask 联动（W2a①）**
  - plan：检出 Caskroom / symlink / `brew list` 后标记 `uninstall:brew-cask:{zap|nozap}:{token}`
  - sibling → `nozap`；否则 `--zap`
  - apply：`brew uninstall --cask`（NONINTERACTIVE）；path↔token 重绑；失败且 cask 仍登记 → skip；仅 cask 已卸才回退 `mole_delete`
  - 保护层 / TOCTOU 不绕过

## 承接

- 含 **1.32.0** Filo production Cache、**1.31.0** optimize DNS/mDNS、**1.29.0** 本地快照报告

## 长尾（仍未在 uninstall）

- login items、系统 LaunchDaemons、`/Library` sudo 残留（W2a②③）

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
