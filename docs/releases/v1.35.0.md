# v1.35.0

## 新增

- **`vole uninstall` 系统 LaunchDaemons / `/Library` sudo 残留主路径（W2a③）**
  - plan：侧车 `uninstall:system-leftover:{kind}:{path}`（LaunchDaemons/Agents、PHT、窄 `/Library` exact 叶、receipts）
  - sibling → 整批系统残留不进 plan；`com.apple.*` 永不入选
  - apply：复用既有 `PrivilegeBackend`（`sudo -n`；TTY 至多一次 `sudo -v`）；plist 先 `launchctl unload` 再 permanent 删除；无凭证 → NeedsPrivilege + 响亮 skip
  - Mole 广谱边缘（Frameworks/kext/Plug-Ins 等）仍记 coverage，本刀不做

## 承接

- 含 **1.34.0** Login Items、**1.33.0** Homebrew Cask、**1.32.0** Filo、**1.31.0** optimize DNS/mDNS

## 长尾（仍未在 uninstall）

- Mole `find_app_system_files` 广谱边缘（Frameworks / kext / Plug-Ins / Input Methods / …）

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
