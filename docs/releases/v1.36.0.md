# v1.36.0

## 新增

- `vole optimize`：`memory_pressure_relief` 进入主路径（W2b②）
  - plan sentinel；apply 仅在内存压力 warning/critical 时 `sudo -n purge`
  - 低压 noop 成功（对齐 Mole「already optimal」）
  - 无凭证 → NeedsPrivilege + 响亮提示；经既有 `PrivilegeBackend`

## 承接

- 含 **1.35.0** 系统 LaunchDaemons、**1.34.0** Login Items、**1.31.0** optimize DNS/mDNS

## 仍未移植（optimize 长尾）

- network_stack / disk_permissions / periodic / spotlight* / disk_verify / login_items / shared_file_list

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
- Mole `find_app_system_files` 广谱 `/Library` 边缘
