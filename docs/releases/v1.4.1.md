# v1.4.1 — orphan FDA / Library 不可访问响亮提示

**日期**：2026-08-05  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：Developer ID Application（tag 触发 Release 流水线）  
**公证**：Apple Notary Service

## 亮点

- **`orphaned-app-data` 权限降级不再静默**：`~/Library/Caches` 不可读或安装扫描失败时：
  - NDJSON：`skipped` + `reason: tcc_denied`
  - `--json` / `--plan-out`：当次 `coverage_note` 追加警告
  - 人读 `--plan`：stderr 一行可操作提示（完全磁盘访问权限）
- 零协议 bump（复用既有 `TccDenied`；语义含规则级 Library 不可访问）

## 非目标（本版）

- 不新增 `SkipReason` 变体
- 不改 uninstall / optimize 权限文案
- system services orphan / Containers stubs / 真 sudo

## 文档

- 设计：[specs/2026-08-05-1919-orphan-fda-loud-hint-design.md](../wukong-code/specs/2026-08-05-1919-orphan-fda-loud-hint-design.md)
- 验收：[findings/2026-08-orphan-fda-loud-hint.md](../findings/2026-08-orphan-fda-loud-hint.md)

## 安装

```bash
brew tap wukongnotnull/vole https://github.com/wukongnotnull/vole
brew install vole
# 已装：
brew upgrade vole
```
