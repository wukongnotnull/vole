# v1.4.2 — uninstall / optimize apply 权限响亮提示

**日期**：2026-08-05  
**平台**：macOS 12+（Apple Silicon / Intel）  
**签名**：Developer ID Application（tag 触发 Release 流水线）  
**公证**：Apple Notary Service

## 亮点

- **`vole uninstall --apply` / `vole optimize --apply`**：若跳过原因含 `tcc_denied` 或 `needs_privilege`，人读 stderr 与 `--json` `coverage_note` 追加可操作中文提示（FDA + 系统保护 / sudo 指引）
- 复用 `vole-core` 共享常量；不改 plan 期英/混 coverage；零协议 bump

## 非目标（本版）

- 不改 clean orphan 既有逻辑（1.4.1）
- 不重写 uninstall/optimize plan 覆盖说明
- 真 sudo / system services orphan

## 文档

- 设计：[specs/2026-08-05-2122-apply-permission-loud-hint-design.md](../wukong-code/specs/2026-08-05-2122-apply-permission-loud-hint-design.md)

## 安装

```bash
brew upgrade vole
```
