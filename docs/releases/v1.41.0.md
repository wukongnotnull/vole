# v1.41.0

## 新增

- **`antigravity-browser-siblings`**：补齐 Antigravity Gemini browser profile 兄弟缓存（Code/GPU/Dawn* / Graphite / crx / Service Worker）
- **`chrome-devtools-mcp-siblings`**：补齐 Chrome DevTools MCP profile 对称兄弟缓存
- **`qq-music-mac-as-caches`**：QQ Music Mac 容器 AS 下 `iRRCache` / `iLog` / `iCache` / `iTemp`（不碰 `iDownloadProxy`）
- 保护层：扩 Chromium sibling 路径段；QQ Music AS 仅放行上述四目录

## 规模

- 清理规则 **537 → 540**

## 仍未移植

- 桌面 SMAppService / 特权助手
- optimize spotlight* / `disk_verify` / `login_items_audit` / `shared_file_list`（长尾 coverage）
