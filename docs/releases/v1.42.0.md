# v1.42.0

## 新增

- `vole optimize`：`login_items_audit` 进入主路径（闸控轨 G1）
  - plan：AppleScript 快照登录项 + 存在性探测；仅列出损坏项
  - apply：只确认、**不删除**（删除仍属 `uninstall` Login Items）
  - 禁止默认弹 GUI 的非特权 `sfltool dumpbtm`；仅 `sudo -n` 可用时才作 BTM 回退
  - `VOLE_TEST_NO_AUTH=1` / 探测失败 → fail-closed + 响亮提示

## 承接

- 含 **1.41.0** Batch6 兄弟路径 / QQ Music AS 等

## 仍未移植（optimize 长尾）

- spotlight* / disk_verify / shared_file_list_repair

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
