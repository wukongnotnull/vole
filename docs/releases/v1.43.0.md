# v1.43.0

## 新增

- `vole optimize`：`spotlight_orphan_rules_cleanup` 进入主路径（闸控轨 G2）
  - plan：扫描用户域 Spotlight `EnabledPreferenceRules`；仅对确认已卸载 app 的 reverse-DNS 规则出候选
  - apply：经 `defaults` 重写 keep 数组（或删除空键）；**不**直接改 plist 绕过 cfprefsd
  - 永久保留 `System.*` / `com.apple.*`；非 reverse-DNS / 探测不确定 → keep（fail-closed）
  - `VOLE_TEST_NO_AUTH=1` → Live 空 plan / apply Skipped

## 承接

- 含 **1.42.0** `login_items_audit` 等

## 仍未移植（optimize 长尾）

- spotlight_index_optimize / disk_verify / shared_file_list_repair

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
