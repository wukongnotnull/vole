# v1.44.0

## 新增

- `vole optimize`：`spotlight_index_optimize` 进入主路径（闸控轨 G3）
  - plan：始终 1 条 sentinel（不探测、不 sudo）
  - apply：Indexing enabled + AC 供电 + 双探针皆慢 → `sudo -n mdutil -E /`
  - 低压 / 健康 / 禁用索引 → noop；无凭证 → NeedsPrivilege（响亮）
  - 与 `system_maintenance` 去重：后者仅只读 `mdutil -s`，本任务独占重建
  - `VOLE_TEST_NO_AUTH=1` 下永不真 sudo

## 承接

- 含 **1.43.0** `spotlight_orphan_rules_cleanup` 等

## 仍未移植（optimize 长尾）

- disk_verify / shared_file_list_repair

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
