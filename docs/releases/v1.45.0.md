# v1.45.0

## 新增

- `vole optimize`：`shared_file_list_repair` 进入主路径（闸控轨 G4）
  - plan：扫描用户域 `com.apple.sharedfilelist` 下 `.sfl2`/`.sfl3`；仅 `plutil -lint` 失败者出候选
  - apply：再 lint 后删除损坏文件；已健康 → noop
  - 永久跳过 `ApplicationRecentDocuments`；**禁止** `sfltool`（含 dumpbtm）
  - 与 clean `recent-items-list` 边界：clean 清空固定 Recent* 列表；本任务仅修损坏共享列表
  - `VOLE_TEST_NO_AUTH=1` → Live 空 plan / apply Skipped

## 承接

- 含 **1.44.0** `spotlight_index_optimize` 等

## 仍未移植（optimize 长尾）

- disk_verify（默认永不升必做）

## 仍未移植（全局）

- 桌面 SMAppService / 特权助手
