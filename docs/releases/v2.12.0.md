# v2.12.0

T8：status 抛光（动画 cat + `k`/`c` prefs）+ `optimize --whitelist` + README / 对照表收口。

## 新增 / 相对 2.11.0

- **`vole optimize --whitelist`**：mole 独立任务白名单（`~/.config/mole/whitelist_optimize`）；TTY 分页多选；`--whitelist-add` / `--remove` / `--list`；plan/apply 跳过所列任务 id
- **`vole status` 动画 cat**：ASCII mole 行走；`k` 隐藏/显示并持久化；`c` 循环 CPU 核心展示数（`2→4→8→all`）；prefs 写 `~/.config/mole/status_prefs`
- **诚实 footer**：`K Cat | C Cores | Q/Esc/Ctrl+C Quit`
- **文档收口**：README TUI 表与成熟度勾满 T5–T8；去掉「余项 T8 / 有意未接线 status cat / optimize --whitelist」等陈旧措辞

## 不变

- `schema_version` **不 bump**
- clean whitelist、删除漏斗、自动化 `--plan` / `--json*` 路径

## 有意不做

- status 其它未列键（仅 Q / K / C）
- bash 像素级光标重绘

## 验收参考

```bash
VOLE_TEST_NO_AUTH=1 cargo test -p vole-core -p vole-cli
./scripts/check-command-surface.sh --enforce
cargo fmt --all -- --check
```

TTY 手工：`vole status` 见 cat、`k`/`c` 生效且重启保留；`vole optimize --whitelist` 管理任务并影响后续 plan。

## Formula / 预编译

发布 GitHub Release 资产后，运行 `bash scripts/update-homebrew-formula.sh 2.12.0` 刷新 `Formula/vole.rb` sha256。
