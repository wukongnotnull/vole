# v2.16.0

TUI polish (home/status vole, warm palette) + uninstall feedback + English full CLI help.

## Added / changed since 2.15.0

- **Home menu**: animated vole beside VOLE ASCII; URL/tagline below brand row
- **Status**: vole above Status Health header; warm brown/coral/meadow accents; body text inherits terminal foreground
- **Uninstall**: mole-style stderr spinner while scanning apps / measuring sizes
- **Paginated select**: footer shows `↑↓` and `B Back` (returns to bare `vole` home)
- **CLI help**: `vole --help` / `-h` / `help` expands every subcommand Usage + Options; English-only copy; section separators; no mole mentions

## Unchanged

- Delete funnel semantics (protection + Trash)
- Plan / apply JSON shapes and `schema_version`
- **No** `schema_version` bump

## Verification

```bash
VOLE_TEST_NO_AUTH=1 cargo test -p vole-core -p vole-cli --bin vole
./scripts/check-command-surface.sh --enforce
cargo fmt --all -- --check
./target/debug/vole --help | less
```

## Formula / prebuilt

After GitHub Release assets exist, run `bash scripts/update-homebrew-formula.sh 2.16.0` to refresh `Formula/vole.rb` sha256.
