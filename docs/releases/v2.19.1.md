# v2.19.1

Human `vole clean` plan explains the highlighted reclaimable total.

## Added / changed since 2.19.0

- **Clean plan (human)**: two lines under `17.1 GB · N groups · N types · N items` — the size is a candidate total (caches / logs / leftovers), not already-freed space; confirm defaults to Trash

## Unchanged

- Delete funnel semantics (protection + Trash)
- Plan / apply JSON shapes and `schema_version`
- **No** `schema_version` bump

## Verification

```bash
VOLE_TEST_NO_AUTH=1 cargo test -p vole-cli --bin vole clean_group::
./scripts/check-command-surface.sh --enforce
cargo fmt --all -- --check
```

## Formula / prebuilt

After GitHub Release assets exist, run `bash scripts/update-homebrew-formula.sh 2.19.1` to refresh `Formula/vole.rb` sha256.
