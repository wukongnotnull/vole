# v2.8.0

TUI 交互 mole 级复刻收口：T0–T3（uninstall / purge / installer / whitelist 双轨多选 + status/analyze 视觉同构）合入后的文档对照、长尾清理与 MINOR 发版。

## 新增 / 相对 2.7.0

- **T1**：TTY 裸 `vole purge` / `vole installer` → 分页多选 → 确认 → 既有 `apply_*_plan`（自动化 path 不变）
- **T2**：`vole clean --whitelist` TTY 切到共享 `PaginatedMultiSelect`（预定义目录 + 预选置顶；flag 自动化不变）
- **T3**：`vole status` / `vole analyze` TUI 视觉同构（区块标签、双列/窄屏、诚实 footer；JSON 零破坏）
- **T4**：对照表勾选 + README「TUI 交互对齐」+ 清除「无 TTY 多选」类 coverage/findings 长尾措辞

## 承接（已在 2.7.0）

- 共享 `PaginatedMultiSelect` + TTY 裸 `vole uninstall` 交互卸载

## 有意不做（诚实长尾）

- analyze：Space 多选、⌫ 删除、O/P、`/` Filter、T Top 等未接线键
- status：mole 动画 cat、`k`/`c` prefs
- bash 增量光标重绘像素级复刻；optimize `--whitelist`
- **不 bump** `schema_version`

## 不变

- `--plan` / `--apply` / `--json*` / 非 TTY：自动化行为不变
- 删除一律经既有 apply 漏斗（废纸篓默认 / 保护 / oplog）

## 验收参考

- `cargo test -p vole-cli`（menu / paginated / uninstall / purge / installer / whitelist / status·analyze layout）
- `cargo test -p vole-core`（相关 plan/apply / whitelist）
- TTY 手工：四命令取消不删；whitelist 取消不写盘；status/analyze 宽窄屏一眼

## Formula / 预编译

发布 GitHub Release 资产后，运行 `bash scripts/update-homebrew-formula.sh 2.8.0` 刷新 `Formula/vole.rb` sha256；本 PR 已 bump 版本与 URL，sha 钉待资产就绪后 pin。
