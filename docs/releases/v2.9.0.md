# v2.9.0

T5：裸 `vole` ratatui 首页与 mole `interactive_main_menu` 同构（品牌 / 五项 / footer / exec 式启动）。

## 新增 / 相对 2.8.0

- **裸 `vole`**：ratatui 首页 — ASCII「Vole」+ `https://github.com/wukongnotnull/vole` + tagline；五项 Clean / Uninstall / Optimize / Analyze / Status（文案对齐 mole）
- **键位**：↑↓ / `1`–`5` / Enter；`M` help · `V` version；条件 `T` TouchID（未配置）/ `U` Update（本地 cache）；`Q` / Esc / Ctrl+C 退出
- **启动语义**：Enter / 数字后 **exec 式**拉起裸子命令（argv **无**硬编码 `--plan`），不回菜单
- **更新条**：仅读 `~/.cache/vole/update_message`（或 `$VOLE_CACHE_DIR`）；由 `vole update --check` 写入；裸入口**不**联网

## 诚实边界

- 首页 Enter 进 **Clean / Optimize** 时仍为既有 plan-only 行为；TTY 确认执行见后续 **T6**
- Uninstall / Analyze / Status 已接既有 TTY 交互或 TUI

## 有意不做（T6–T8）

- T6：Clean/Optimize TTY 确认双轨（plan → 确认 → apply）
- T7：analyze Space/⌫/O/P/`/`/T 进阶键
- T8：status 动画 cat / prefs；`optimize --whitelist`；对照表收口
- **不 bump** `schema_version`

## 不变

- 已带子命令路径行为不变；非 TTY 裸 `vole`：stderr + exit 2
- 删除编排仍在 `vole-core`；首页只做导航壳

## 验收参考

- `cargo test -p vole-cli home_menu_state`
- `cargo test -p vole-cli home_menu`
- `cargo test -p vole-cli --test interactive_cli`
- TTY 手工：`cargo run -p vole-cli` 看首页；Q 退出后 shell 正常

## Formula / 预编译

发布 GitHub Release 资产后，运行 `bash scripts/update-homebrew-formula.sh 2.9.0` 刷新 `Formula/vole.rb` sha256。
