# Private Var DB Powerlog Implementation Plan

**Goal:** `private-var-db-powerlog`（1.18.0）— 对齐 DiagnosticPipeline 实现。

### Task 1
谓词 + Privilege + plan/apply + TOML + coverage + 1.18.0 docs；三树回归。

### Task 2
`cargo test -p vole-core --lib`；PR + security-review；CI 绿后 merge。
