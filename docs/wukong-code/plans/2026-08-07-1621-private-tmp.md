# Plan: private-tmp（1.21.0）

**Goal:** `private-tmp` — `/private/tmp` + `/private/var/tmp` 深度 1、≥7d、sudo -n。

谓词 `is_private_tmp_clean_target`；Privilege candidates（双 walk）；plan/apply；TOML `older_than_days=7`；coverage；1.21.0。
