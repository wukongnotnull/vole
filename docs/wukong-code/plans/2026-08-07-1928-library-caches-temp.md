# Plan: library-caches-temp（1.22.0）

**Goal:** `library-caches-temp` — `/Library/Caches` ≤5、`.cache`/`.tmp`/`.log`、≥7d、sudo -n（对齐 Mole 全树）。

谓词 `is_library_caches_temp_clean_target`；Privilege candidates；plan/apply；TOML；coverage；1.22.0。
