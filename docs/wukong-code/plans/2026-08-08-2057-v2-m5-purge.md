# Vole M5：`purge` + 别名 → 2.0.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use wukong-code:executing-plans（本仓默认 inline）或 wukong-code:subagent-driven-development。Steps 用 checkbox（`- [ ]`）跟踪。

**Goal:** 实现高对齐 `vole purge`（plan/apply/JSON/保护/废纸篓/菜单/补全）并同 PR 补 §3.3 别名，发版 **2.0.0**。

**Architecture:** 镜像 uninstall：`vole-core::ops::{purge_plan,purge_apply}` 产出/消费 `ProtoPlan`（`rule_id` 前缀 `purge:`），删除经 `mole_delete_verified`；CLI 薄前端。发现逻辑对照 Mole `purge_shared.sh` / `project.sh` 主路径子集。

**Tech Stack:** Rust workspace、`walkdir`/`jwalk`（若已有则复用）、`vole-proto` schema 1、Mole 钉版 targets 表。

**Design:** [`../specs/2026-08-08-2057-v2-m5-purge-design.md`](../specs/2026-08-08-2057-v2-m5-purge-design.md)

## Global Constraints

- 权威续篇规格：`docs/wukong-code/specs/2026-08-08-2030-v2-cli-complete-design.md`
- Mole 钉版：`third_party/mole-1.48.1`；targets **原样钉死**，不盲扩
- 删除只走 `mole_delete_verified`；禁止平行 `rm -rf`
- **不 bump** `schema_version`；包版本在功能就绪后升 **2.0.0**
- TDD：先红测再实现；每 Task commit
- 合入用 merge commit（非 squash）

## File map

| 文件 | 职责 |
|---|---|
| `crates/vole-core/src/ops/purge_plan.rs` | 发现 + `build_purge_plan` |
| `crates/vole-core/src/ops/purge_apply.rs` | TTL/TOCTOU/`mole_delete_verified` |
| `crates/vole-core/src/ops/mod.rs` | 模块导出 |
| `crates/vole-core/src/mutex.rs` | `try_lock_purge` |
| `crates/vole-cli/src/purge.rs` | CLI 接线 |
| `crates/vole-cli/src/main.rs` | `Command::Purge` + 别名 |
| `crates/vole-cli/src/interactive.rs` | 菜单项 |
| `crates/vole-cli/tests/purge_cli.rs` | CLI 测 |
| `docs/releases/v2.0.0.md` | 发版说明 |
| `Cargo.toml` / `Formula/vole.rb` / `README.md` | 版本 |

---

### Task 1: `purge_plan` 红测 + 发现实现

**Files:**
- Create: `crates/vole-core/src/ops/purge_plan.rs`
- Modify: `crates/vole-core/src/ops/mod.rs`

- [ ] **Step 1: 写失败单测**（temp 树：陈旧 `node_modules` + 新 `target`；仅前者进 plan）

- [ ] **Step 2: 实现 `PURGE_TARGETS` / 搜索根 / `build_purge_plan`**

- [ ] **Step 3: `cargo test -p vole-core purge` 绿**

- [ ] **Step 4: Commit** `feat: add purge plan discovery for project artifacts`

---

### Task 2: `purge_apply` 红测 + 漏斗

**Files:**
- Create: `crates/vole-core/src/ops/purge_apply.rs`
- Modify: `crates/vole-core/src/ops/mod.rs`、`mutex.rs`

- [ ] **Step 1: 测拒绝非 `purge:`；测 trash apply（`MOLE_TEST_TRASH_DIR`）**

- [ ] **Step 2: 实现 `apply_purge_plan`**

- [ ] **Step 3: 测试绿 + Commit** `feat: apply purge plans via delete funnel`

---

### Task 3: CLI + 菜单 + 别名

**Files:**
- Create: `crates/vole-cli/src/purge.rs`、`tests/purge_cli.rs`
- Modify: `main.rs`、`interactive.rs`

- [ ] **Step 1: CLI 测红**（`--help` / `--plan --json`）

- [ ] **Step 2: 接线 `Command::Purge` + 别名 visible_alias**

- [ ] **Step 3: 菜单加 purge；测试绿**

- [ ] **Step 4: Commit** `feat: expose vole purge CLI with aliases`

---

### Task 4: 2.0.0 发版文档与版本 bump

**Files:**
- Create: `docs/releases/v2.0.0.md`
- Modify: `Cargo.toml`、`Formula/vole.rb`、`README.md`（版本提及处）

- [ ] **Step 1: 确认 purge/别名测试绿**

- [ ] **Step 2: bump `2.0.0` + release notes**

- [ ] **Step 3: `./scripts/check-command-surface.sh` 确认 purge+三别名不再 MISSING**

- [ ] **Step 4: Commit** `release: vole 2.0.0 with purge and command aliases`

---

## Self-review

1. Spec coverage：design §2–§7 均有 Task  
2. 无 TBD  
3. `rule_id` 前缀全程 `purge:`  

---

默认 **Inline** 执行（executing-plans）。
