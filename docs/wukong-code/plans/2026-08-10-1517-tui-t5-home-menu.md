# T5：裸 `vole` ratatui 首页（mole 同构）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use wukong-code:executing-plans（本仓默认 inline）或 wukong-code:subagent-driven-development。Steps 用 checkbox（`- [ ]`）跟踪。

**Goal:** 用 ratatui 首页替换 `interactive.rs` 数字菜单，对齐 mole `interactive_main_menu`（品牌 / 五项 / footer / exec 式启动）；发版 **2.9.0**。

**Architecture:** 纯逻辑 `HomeMenuState`（可单测）+ `tui::home_menu` 渲染循环；复用 `Theme`、`TerminalGuard`、`drain_pending_input`。进入前 drain；选中后恢复终端并以 exec 式拉起裸子命令（不回菜单）。**不**复用 `PaginatedMultiSelect`。

**Tech Stack:** Rust 1.97.1、`ratatui` 0.30、`crossterm` 0.28、既有 `TerminalGuard` / `Theme`、`vole_core::ops::{is_touchid_configured, resolve_touchid_paths}`。

**Design:** [`../specs/2026-08-10-1514-tui-t5-home-menu-roadmap-design.md`](../specs/2026-08-10-1514-tui-t5-home-menu-roadmap-design.md)

## Global Constraints

- Mole 钉版：`third_party/mole-1.48.1`（入口 `interactive_main_menu` / `show_main_menu` / `_main_menu_controls_line`）
- 首页只做导航壳；编排 / 删除 / 保护仍在 `vole-core`；禁止第二条删除路径
- **不 bump** `schema_version`；功能就绪后包版本 **2.9.0**（相对当前 `2.8.0`）
- Clean / Optimize Enter 后仍走现有 plan-only 路径（诚实边界，直到 T6）；README / help 必须写明
- 裸入口**不**后台联网 `check_for_updates`（M4/M9 故意差异保留）；更新条仅在本地 cache 非空时显示
- Footer 条件键对齐 mole：`T TouchID` 仅未配置时显示；若已配置且有更新条则显示 `U Update`（`elif`，二者不同时出现在 footer）；`U` 键仅在有更新条时生效；`T` 键始终可进 `touchid`
- TDD：先红再绿；每 Task 一次 commit
- 合入用 merge commit（非 squash）；CI 绿后按 `pr-auto-merge-when-ci-green` 自动合并
- 测试：`VOLE_TEST_NO_AUTH=1`；禁止交互路径挂真 sudo / Touch ID
- **本 plan 范围仅 T5**；T6–T8 见 design 路标，**不写实现 Task**

## Out of scope（T6–T8，仅路标）

| 波次 | 内容 | 本 plan |
|---|---|---|
| T6 | Clean/Optimize TTY 确认双轨（plan→确认→apply） | 不做 |
| T7 | analyze Space/⌫/O/P/`/`/T 进阶键 | 不做 |
| T8 | status 动画 cat / prefs；`optimize --whitelist`；对照表收口 | 不做 |

## File map

| 文件 | 职责 |
|---|---|
| `crates/vole-cli/src/tui/home_menu_state.rs` | 纯逻辑：五项光标、键位 → `HomeAction`；footer 可见性 |
| `crates/vole-cli/src/tui/home_menu.rs` | ASCII 品牌、ratatui 循环、`map_key`、调用 drain + `TerminalGuard` |
| `crates/vole-cli/src/tui/mod.rs` | 模块导出 |
| `crates/vole-cli/src/interactive.rs` | TTY 门控；装配 touchid/update；`run_home_menu`；exec 式启动；M/V 清屏后 exit |
| `crates/vole-cli/src/update_banner.rs`（或放在 `update.rs` 内 helper） | 读/写 `~/.cache/vole/update_message`（无网络） |
| `crates/vole-cli/src/update.rs` | `--check` 发现可更新时写入 cache；已最新则清空 |
| `crates/vole-cli/src/main.rs` | `after_help` 文案（首页非「simple menu」） |
| `crates/vole-cli/tests/interactive_cli.rs` | 非 TTY 回归；help/after_help 含首页/T6 诚实句（若可断言） |
| `README.md` / `docs/releases/v2.9.0.md` / `Cargo.toml` / `Formula/vole.rb` | 文档与版本 |

---

### Task 1: `HomeMenuState` 纯逻辑

**Files:**
- Create: `crates/vole-cli/src/tui/home_menu_state.rs`
- Modify: `crates/vole-cli/src/tui/mod.rs`

**Interfaces:**
- Produces:
  - `pub const HOME_ITEMS: [HomeItem; 5]`（标题+描述常量）
  - `pub struct HomeItem { pub title: &'static str, pub description: &'static str }`
  - `pub enum HomeCommand { Clean, Uninstall, Optimize, Analyze, Status, TouchId, Update }`
  - `impl HomeCommand { pub fn argv(self) -> &'static [&'static str] }`  
    - Clean→`["clean"]`，Uninstall→`["uninstall"]`，Optimize→`["optimize"]`，Analyze→`["analyze"]`，Status→`["status"]`，TouchId→`["touchid"]`，Update→`["update"]`（**无** `--plan`）
  - `pub enum HomeKey { Up, Down, Enter, Digit(u8), More, Version, TouchId, Update, Quit }`
  - `pub enum HomeAction { Launch(HomeCommand), ShowHelp, ShowVersion, Quit }`
  - `pub struct HomeMenuConfig { pub touchid_configured: bool, pub show_update: bool }`
  - `pub struct HomeMenuState { /* cursor: usize 0..4, cfg */ }`
  - `pub fn new(cfg: HomeMenuConfig) -> Self`（cursor=0）
  - `pub fn cursor(&self) -> usize`
  - `pub fn handle_key(&mut self, key: HomeKey) -> Option<HomeAction>`
  - `pub fn footer_shows_touchid(&self) -> bool`（`!touchid_configured`）
  - `pub fn footer_shows_update(&self) -> bool`（`touchid_configured && show_update`，对齐 mole `elif`）
  - `pub fn controls_line(&self) -> String`（subtle 文案拼接，供渲染/测）

- [ ] **Step 1: 写失败单测**（`home_menu_state.rs` 内 `#[cfg(test)]`）

```rust
#[test]
fn home_items_match_mole_copy() {
    assert_eq!(HOME_ITEMS[0].title, "Clean");
    assert_eq!(HOME_ITEMS[0].description, "Free up disk space");
    assert_eq!(HOME_ITEMS[1].title, "Uninstall");
    assert_eq!(HOME_ITEMS[1].description, "Remove apps completely");
    assert_eq!(HOME_ITEMS[2].title, "Optimize");
    assert_eq!(HOME_ITEMS[2].description, "Refresh caches and services");
    assert_eq!(HOME_ITEMS[3].title, "Analyze");
    assert_eq!(HOME_ITEMS[3].description, "Explore disk usage");
    assert_eq!(HOME_ITEMS[4].title, "Status");
    assert_eq!(HOME_ITEMS[4].description, "Monitor system health");
}

#[test]
fn arrows_wrap_not_and_digit_launches() {
    let mut st = HomeMenuState::new(HomeMenuConfig {
        touchid_configured: true,
        show_update: false,
    });
    assert_eq!(st.cursor(), 0);
    assert!(st.handle_key(HomeKey::Up).is_none());
    assert_eq!(st.cursor(), 0);
    assert!(st.handle_key(HomeKey::Down).is_none());
    assert_eq!(st.cursor(), 1);
    assert_eq!(
        st.handle_key(HomeKey::Enter),
        Some(HomeAction::Launch(HomeCommand::Uninstall))
    );
    assert_eq!(
        st.handle_key(HomeKey::Digit(1)),
        Some(HomeAction::Launch(HomeCommand::Clean))
    );
    assert_eq!(HomeCommand::Clean.argv(), &["clean"]);
    assert_eq!(HomeCommand::Optimize.argv(), &["optimize"]);
}

#[test]
fn m_v_q_and_conditional_u() {
    let mut st = HomeMenuState::new(HomeMenuConfig {
        touchid_configured: true,
        show_update: false,
    });
    assert_eq!(st.handle_key(HomeKey::More), Some(HomeAction::ShowHelp));
    assert_eq!(st.handle_key(HomeKey::Version), Some(HomeAction::ShowVersion));
    assert_eq!(st.handle_key(HomeKey::Quit), Some(HomeAction::Quit));
    assert!(st.handle_key(HomeKey::Update).is_none()); // 无更新条

    let mut st2 = HomeMenuState::new(HomeMenuConfig {
        touchid_configured: true,
        show_update: true,
    });
    assert_eq!(
        st2.handle_key(HomeKey::Update),
        Some(HomeAction::Launch(HomeCommand::Update))
    );
}

#[test]
fn footer_touchid_vs_update_elif() {
    let a = HomeMenuState::new(HomeMenuConfig {
        touchid_configured: false,
        show_update: true,
    });
    assert!(a.footer_shows_touchid());
    assert!(!a.footer_shows_update()); // mole: T 优先，U 不进 footer
    let line = a.controls_line();
    assert!(line.contains("T TouchID"));
    assert!(!line.contains("U Update"));

    let b = HomeMenuState::new(HomeMenuConfig {
        touchid_configured: true,
        show_update: true,
    });
    assert!(!b.footer_shows_touchid());
    assert!(b.footer_shows_update());
    let line = b.controls_line();
    assert!(!line.contains("T TouchID"));
    assert!(line.contains("U Update"));
}

#[test]
fn touchid_key_always_launches_even_when_configured() {
    let mut st = HomeMenuState::new(HomeMenuConfig {
        touchid_configured: true,
        show_update: false,
    });
    assert_eq!(
        st.handle_key(HomeKey::TouchId),
        Some(HomeAction::Launch(HomeCommand::TouchId))
    );
    assert_eq!(HomeCommand::TouchId.argv(), &["touchid"]);
}
```

- [ ] **Step 2: Run 确认红**

Run: `cargo test -p vole-cli home_menu_state -- --nocapture`  
Expected: FAIL（模块不存在 / 类型未定义）

- [ ] **Step 3: 最小实现**

```rust
// home_menu_state.rs — 要点
pub const HOME_ITEMS: [HomeItem; 5] = [
    HomeItem { title: "Clean", description: "Free up disk space" },
    HomeItem { title: "Uninstall", description: "Remove apps completely" },
    HomeItem { title: "Optimize", description: "Refresh caches and services" },
    HomeItem { title: "Analyze", description: "Explore disk usage" },
    HomeItem { title: "Status", description: "Monitor system health" },
];

impl HomeCommand {
    pub fn argv(self) -> &'static [&'static str] {
        match self {
            Self::Clean => &["clean"],
            Self::Uninstall => &["uninstall"],
            Self::Optimize => &["optimize"],
            Self::Analyze => &["analyze"],
            Self::Status => &["status"],
            Self::TouchId => &["touchid"],
            Self::Update => &["update"],
        }
    }
}

impl HomeMenuState {
    pub fn handle_key(&mut self, key: HomeKey) -> Option<HomeAction> {
        match key {
            HomeKey::Up => { if self.cursor > 0 { self.cursor -= 1; } None }
            HomeKey::Down => { if self.cursor + 1 < 5 { self.cursor += 1; } None }
            HomeKey::Enter => Some(HomeAction::Launch(Self::cmd_at(self.cursor))),
            HomeKey::Digit(d) if (1..=5).contains(&d) => {
                Some(HomeAction::Launch(Self::cmd_at((d - 1) as usize)))
            }
            HomeKey::More => Some(HomeAction::ShowHelp),
            HomeKey::Version => Some(HomeAction::ShowVersion),
            HomeKey::TouchId => Some(HomeAction::Launch(HomeCommand::TouchId)),
            HomeKey::Update if self.cfg.show_update => {
                Some(HomeAction::Launch(HomeCommand::Update))
            }
            HomeKey::Quit => Some(HomeAction::Quit),
            _ => None,
        }
    }

    pub fn controls_line(&self) -> String {
        let mut s = String::from("↑↓  |  Enter  |  M More  |  V Version");
        if self.footer_shows_touchid() {
            s.push_str("  |  T TouchID");
        } else if self.footer_shows_update() {
            s.push_str("  |  U Update");
        }
        s.push_str("  |  Q Quit");
        s
    }
}
```

`mod.rs` 增加 `mod home_menu_state;` 与 `pub use home_menu_state::{...}`。

- [ ] **Step 4: 测试绿 + Commit**

```bash
cargo test -p vole-cli home_menu_state
git add crates/vole-cli/src/tui/home_menu_state.rs crates/vole-cli/src/tui/mod.rs
git commit -m "$(cat <<'EOF'
feat(cli): HomeMenuState for mole-aligned home menu

Pure cursor/key/footer logic with unit tests, independent of
ratatui, for the bare-vole T5 home shell.
EOF
)"
```

---

### Task 2: ratatui `home_menu` 渲染循环 + 键映射

**Files:**
- Create: `crates/vole-cli/src/tui/home_menu.rs`
- Modify: `crates/vole-cli/src/tui/mod.rs`

**Interfaces:**
- Consumes: `HomeMenuState`, `HomeMenuConfig`, `HomeKey`, `HomeAction`, `Theme`, `TerminalGuard`, `drain_pending_input`
- Produces:
  - `pub const VOLE_TAGLINE: &str = "Deep clean and optimize your Mac.";`
  - `pub const VOLE_REPO_URL: &str = "https://github.com/wukongnotnull/vole";`
  - `pub fn brand_ascii_lines() -> [&'static str; 5]`（固定 ASCII「Vole」五行，见下方常量）
  - `pub fn map_key(key: KeyEvent) -> Option<HomeKey>`
  - `pub fn run_home_menu(cfg: HomeMenuConfig) -> io::Result<HomeAction>`

**ASCII 品牌（写入 `brand_ascii_lines`，测等值）：**

```text
__     __    _
\ \   / /___| | ___
 \ \ / / _ \ |/ _ \
  \ V / (_) | |  __/
   \_/ \___/|_|\___|
```

渲染布局（上→下，`Theme::default()`）：

1. 品牌：五行 ASCII 用 `theme.ok`；第 4 行右侧追加 `VOLE_REPO_URL`（`theme.primary` 或 blue 等价）；第 5 行右侧追加 `VOLE_TAGLINE`（`theme.ok`）
2. 若 `cfg.show_update`：空一行后渲染传入的 update 文本（本 Task 的 `run_home_menu` 先只根据 `show_update` 显示占位一行 `"Update available"` **或** 接受额外参数——见 Interfaces 补丁）

**Interfaces 补丁（与 Task 3 对齐）：**

```rust
pub struct HomeMenuRunOpts {
    pub cfg: HomeMenuConfig,
    pub update_message: Option<String>, // Some 非空时渲染更新条；cfg.show_update 应与之同步
}

pub fn run_home_menu(opts: HomeMenuRunOpts) -> io::Result<HomeAction>
```

3. 空行 + 五项：选中 `"> "` + `theme.selected`；未选中 `"  "` + `theme.normal`；标题列宽对齐（如 `Clean`/`Uninstall`… pad 到 12）+ 描述
4. footer：`theme.subtle` + `state.controls_line()`

- [ ] **Step 1: 写失败单测（map_key / brand 常量）**

```rust
#[test]
fn brand_constants() {
    assert_eq!(VOLE_REPO_URL, "https://github.com/wukongnotnull/vole");
    assert_eq!(VOLE_TAGLINE, "Deep clean and optimize your Mac.");
    let lines = brand_ascii_lines();
    assert!(lines[0].contains("__"));
    assert!(lines[3].contains(r"\ V /") || lines[3].contains("V"));
}

#[test]
fn map_crossterm_home_keys() {
    use crossterm::event::{KeyCode, KeyEvent, KeyModifiers};
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Up, KeyModifiers::NONE)),
        Some(HomeKey::Up)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('3'), KeyModifiers::NONE)),
        Some(HomeKey::Digit(3))
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('m'), KeyModifiers::NONE)),
        Some(HomeKey::More)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('V'), KeyModifiers::NONE)),
        Some(HomeKey::Version)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('t'), KeyModifiers::NONE)),
        Some(HomeKey::TouchId)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('u'), KeyModifiers::NONE)),
        Some(HomeKey::Update)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Esc, KeyModifiers::NONE)),
        Some(HomeKey::Quit)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('c'), KeyModifiers::CONTROL)),
        Some(HomeKey::Quit)
    ));
    assert!(matches!(
        map_key(KeyEvent::new(KeyCode::Char('q'), KeyModifiers::NONE)),
        Some(HomeKey::Quit)
    ));
}
```

- [ ] **Step 2: Run 确认红**

Run: `cargo test -p vole-cli home_menu -- --nocapture`  
Expected: FAIL

- [ ] **Step 3: 实现 `run_home_menu`**

```rust
pub fn run_home_menu(opts: HomeMenuRunOpts) -> io::Result<HomeAction> {
    let mut guard = TerminalGuard::enter()?;
    drain_pending_input(Duration::from_millis(200));
    let mut term = Terminal::new(CrosstermBackend::new(stdout()))?;
    let mut state = HomeMenuState::new(opts.cfg);
    let theme = Theme::default();
    loop {
        term.draw(|f| render_home(f, &state, &theme, opts.update_message.as_deref()))?;
        if !event::poll(Duration::from_millis(50))? {
            continue;
        }
        let Event::Key(key) = event::read()? else { continue };
        if key.kind != KeyEventKind::Press {
            continue;
        }
        let Some(hk) = map_key(key) else { continue };
        if let Some(action) = state.handle_key(hk) {
            guard.restore();
            drain_pending_input(Duration::from_millis(100)); // 对齐 mole launch 前 drain
            return Ok(action);
        }
    }
}

fn map_key(key: KeyEvent) -> Option<HomeKey> {
    match key.code {
        KeyCode::Up => Some(HomeKey::Up),
        KeyCode::Down => Some(HomeKey::Down),
        KeyCode::Enter => Some(HomeKey::Enter),
        KeyCode::Esc => Some(HomeKey::Quit),
        KeyCode::Char('c') if key.modifiers.contains(KeyModifiers::CONTROL) => Some(HomeKey::Quit),
        KeyCode::Char(c) => match c {
            '1'..='5' => Some(HomeKey::Digit(c as u8 - b'0')),
            'm' | 'M' => Some(HomeKey::More),
            'v' | 'V' => Some(HomeKey::Version),
            't' | 'T' => Some(HomeKey::TouchId),
            'u' | 'U' => Some(HomeKey::Update),
            'q' | 'Q' => Some(HomeKey::Quit),
            _ => None,
        },
        _ => None,
    }
}
```

**禁止**调用 `run_paginated_select` / `MenuState`。

- [ ] **Step 4: 测试绿 + Commit**

```bash
cargo test -p vole-cli home_menu
git add crates/vole-cli/src/tui/home_menu.rs crates/vole-cli/src/tui/mod.rs
git commit -m "$(cat <<'EOF'
feat(cli): ratatui home menu shell

Render mole-aligned brand, five items, and footer controls with
TerminalGuard plus input drain before returning a HomeAction.
EOF
)"
```

---

### Task 3: 接线 `interactive.rs` + exec 式启动

**Files:**
- Modify: `crates/vole-cli/src/interactive.rs`（整文件替换数字菜单循环）
- Modify: `crates/vole-cli/src/main.rs`（仅 `after_help` 一句，可本 Task 或 Task 5；本 Task 改接线即可）

**Interfaces:**
- Consumes: `tui::run_home_menu`, `HomeMenuRunOpts`, `HomeAction`, `HomeCommand`, `drain_pending_input`, `vole_core::ops::{is_touchid_configured, resolve_touchid_paths}`，以及 Task 4 的 `read_update_message_cache`（若 Task 4 尚未合入：本 Task 先 `update_message: None` / `show_update: false`，Task 4 再接 cache——**推荐先做 Task 4 再合本 Task**；执行时按编号顺序）
- Produces: `pub fn run() -> i32`（行为见下）

**行为契约：**

| 条件 | 行为 |
|---|---|
| 非 TTY（stdin 或 stdout） | stderr 提示 + exit **2**（保持现句：`vole: run with a subcommand...`） |
| TTY | `run_home_menu`；按 `HomeAction` 处理 |
| `Launch(cmd)` | 恢复终端后 `exec_self(cmd.argv())`（unix `CommandExt::exec`）；失败则 stderr + exit 1 |
| `ShowHelp` | 清屏；`clap` 打印顶层 long help（或 `Cli` help）；**exit 0**，不回菜单 |
| `ShowVersion` | 清屏；打印 `vole <CARGO_PKG_VERSION>`（对齐 `--version` 短输出即可）；**exit 0** |
| `Quit` | exit **0** |

```rust
#[cfg(unix)]
fn exec_self(args: &[&str]) -> io::Result<i32> {
    use std::os::unix::process::CommandExt;
    let exe = std::env::current_exe()?;
    let err = Command::new(&exe).args(args).exec();
    Err(io::Error::new(io::ErrorKind::Other, format!("exec {}: {err}", args.join(" "))))
}
```

装配：

```rust
let touchid_configured = is_touchid_configured(&resolve_touchid_paths());
let update_message = read_update_message_cache(); // Task 4
let show_update = update_message.as_ref().is_some_and(|s| !s.trim().is_empty());
let action = tui::run_home_menu(HomeMenuRunOpts {
    cfg: HomeMenuConfig { touchid_configured, show_update },
    update_message,
})?;
```

- [ ] **Step 1: 写/扩展失败集成测**（`tests/interactive_cli.rs`）

保留既有非 TTY 测。新增：

```rust
#[test]
fn top_level_help_mentions_home_menu_not_numbered_plan_list() {
    let output = Command::new(env!("CARGO_BIN_EXE_vole"))
        .args(["--help"])
        .output()
        .expect("help");
    assert!(output.status.success());
    let stdout = String::from_utf8_lossy(&output.stdout);
    // after_help 或 about：指向终端首页，而非暗示 11 项数字菜单
    assert!(
        stdout.to_lowercase().contains("menu") || stdout.contains("terminal"),
        "help={stdout}"
    );
    assert!(
        !stdout.contains("Select [1-11]"),
        "stale numbered menu leaked into help: {stdout}"
    );
}
```

（完整 README/T6 句放 Task 5；本步至少钉住 help 不泄漏旧菜单。）

- [ ] **Step 2: Run 确认红或钉住基线**

Run: `cargo test -p vole-cli --test interactive_cli -- --nocapture`  
Expected: 若 `after_help` 仍写 `simple menu` 可先绿；实现后确认非 TTY 仍 exit 2。

- [ ] **Step 3: 重写 `interactive.rs`**

删除 `loop` + `run_child` 回菜单语义。Help 打印避免再进 raw mode：

```rust
fn print_help_and_exit() -> i32 {
    let _ = io::stdout().write_all(b"\x1b[2J\x1b[H");
    let mut cmd = crate::build_cli_command(); // 见下：从 main 抽出或在此用 clap
    let _ = cmd.print_long_help();
    0
}
```

若不愿抽 `build_cli_command`：在 `interactive` 内用最小文案列出主命令 + 一句「详见 `vole --help`」，**但优先**复用 clap（可在 `main.rs` 增加 `pub(crate) fn clap_command() -> clap::Command { Cli::command() }`）。

- [ ] **Step 4: 测试绿 + Commit**

```bash
cargo test -p vole-cli --test interactive_cli
cargo test -p vole-cli home_menu
git add crates/vole-cli/src/interactive.rs crates/vole-cli/src/main.rs crates/vole-cli/tests/interactive_cli.rs
git commit -m "$(cat <<'EOF'
feat(cli): bare vole launches ratatui home menu

Replace the numbered Select[1-11] loop with exec-style home
navigation isomorphic to mole interactive_main_menu.
EOF
)"
```

---

### Task 4: 更新条 cache（只读首页 + `--check` 写入）

**Files:**
- Create: `crates/vole-cli/src/update_banner.rs` **或** 在 `update.rs` 增加 `pub(crate)` helpers
- Modify: `crates/vole-cli/src/main.rs`（`mod update_banner;` 若新建）
- Modify: `crates/vole-cli/src/update.rs`（`--check` 结果写 cache）
- Modify: `crates/vole-cli/src/interactive.rs`（读 cache；若 Task 3 已占位则替换）

**Interfaces:**
- Produces:
  - `pub fn update_message_cache_path() -> PathBuf`  
    - `$VOLE_CACHE_DIR/update_message` 若设了 `VOLE_CACHE_DIR`；否则 `$HOME/.cache/vole/update_message`
  - `pub fn read_update_message_cache() -> Option<String>`（文件缺失/空/仅空白 → `None`；否则 `Some(内容)`）
  - `pub fn write_update_message_cache(msg: Option<&str>) -> io::Result<()>`（`None` 或空 → 写空文件或删除；`Some` → 写入人类可读一行，如 `Update 2.9.1 available, run vole update`）

**硬约束：** `interactive::run` / `run_home_menu` **不得**发起网络请求。

- [ ] **Step 1: 写失败单测**（`update_banner.rs` 或 `update.rs` 的 `#[cfg(test)]`，用 `tempdir` + env）

```rust
#[test]
fn read_write_update_message_cache_roundtrip() {
    let dir = tempfile::tempdir().unwrap();
    std::env::set_var("VOLE_CACHE_DIR", dir.path());
    assert!(read_update_message_cache().is_none());
    write_update_message_cache(Some("Update 9.9.9 available, run vole update")).unwrap();
    let msg = read_update_message_cache().unwrap();
    assert!(msg.contains("9.9.9"));
    write_update_message_cache(None).unwrap();
    assert!(read_update_message_cache().is_none());
    std::env::remove_var("VOLE_CACHE_DIR");
}
```

（若并行测竞态：测内用唯一子目录 path 参数重载 `read/write_at(path)`，再包一层默认 path——优先可注入 path 的内部 API：`read_update_message_cache_at(&Path)`。）

- [ ] **Step 2: Run 确认红**

Run: `cargo test -p vole-cli read_write_update_message_cache -- --nocapture`  
Expected: FAIL

- [ ] **Step 3: 实现 + 在 `run_update_inner` 的 check 分支写入**

当 `UpdateOutcome::Check { current, latest, .. }` 且 `latest` 语义上新于 `current` 时写入提示；`AlreadyLatest` / 无需更新时清空 cache。  
**不要**在裸 `vole` 入口调用 check。

- [ ] **Step 4: 测试绿 + Commit**

```bash
cargo test -p vole-cli read_write_update_message_cache
cargo test -p vole-cli --test interactive_cli
git add crates/vole-cli/src/update_banner.rs crates/vole-cli/src/update.rs crates/vole-cli/src/main.rs crates/vole-cli/src/interactive.rs
git commit -m "$(cat <<'EOF'
feat(cli): optional home update banner from local cache

Read ~/.cache/vole/update_message for the home footer U shortcut;
write it from vole update --check without networking on bare vole.
EOF
)"
```

---

### Task 5: README + help 诚实边界（Clean/Optimize → T6）

**Files:**
- Modify: `README.md`（「TUI 交互对齐」表 + 共享组件段）
- Modify: `crates/vole-cli/src/main.rs`（`after_help`）
- Modify: `crates/vole-cli/tests/interactive_cli.rs`（断言帮助/文档关键句若适合）

- [ ] **Step 1: 更新 README 片段**

在「TUI 交互对齐」表**顶部**增一行：

```markdown
| 裸 `vole`（无子命令） | ratatui 首页：品牌 + Clean/Uninstall/Optimize/Analyze/Status；↑↓/数字/Enter；M help·V version·条件 T/U；Q 退出；Enter 后 **exec 式**进子命令（不回菜单） | 非 TTY：stderr + exit 2；已带子命令：不变 |
```

在表下说明中追加诚实句（原样可用）：

```markdown
首页 Enter 进 **Clean / Optimize** 时目前仍为既有 plan-only 行为（TTY 确认执行见后续 T6 / 下一版本）；Uninstall / Analyze / Status 已接 TTY 交互或 TUI。
```

更新成熟度/版本句等到 Task 6 一并改亦可；本 Task 至少消灭「首页仍是数字菜单」暗示。

- [ ] **Step 2: 改 `after_help`**

```rust
after_help = "Run `vole` with no subcommand in a terminal to open the home menu (mole-style). Clean/Optimize still plan-only until the confirm track ships."
```

- [ ] **Step 3: Commit**

```bash
git add README.md crates/vole-cli/src/main.rs crates/vole-cli/tests/interactive_cli.rs
git commit -m "$(cat <<'EOF'
docs: document ratatui home menu and Clean plan-only caveat

README TUI table and clap after_help describe bare-vole home
navigation and point Clean/Optimize confirm execution to T6.
EOF
)"
```

---

### Task 6: 发版 2.9.0

**Files:**
- Modify: `Cargo.toml`（workspace `version = "2.9.0"`）
- Create: `docs/releases/v2.9.0.md`
- Modify: `README.md` 版本钉（当前 `2.8.0` → `2.9.0` 文案；预编译 URL 可先占位或待资产）
- Modify: `Formula/vole.rb`（`version "2.9.0"`；sha256 若无资产则保持注释说明「发布后 `scripts/update-homebrew-formula.sh 2.9.0`」——按仓内 T0/T4 惯例至少 bump version 字段）

- [ ] **Step 1: 写 `docs/releases/v2.9.0.md`**

要点：

- 裸 `vole`：ratatui 首页与 `mo` 同构（ASCII Vole + URL + tagline；五项；footer）
- exec 式启动子命令（不回菜单）；argv **无**硬编码 `--plan`
- 条件键：T（TouchID 未配置）/ U（本地 update cache）
- 诚实：Clean/Optimize 仍 plan-only，确认双轨见 T6
- **不 bump** `schema_version`
- 有意不做：T6–T8 内容

- [ ] **Step 2: bump 版本 + 测**

```bash
# Cargo.toml: version = "2.9.0"
cargo test -p vole-cli home_menu_state
cargo test -p vole-cli home_menu
cargo test -p vole-cli --test interactive_cli
```

Expected: PASS；确认无 `schema_version` 常量被改。

- [ ] **Step 3: Commit**

```bash
git add Cargo.toml docs/releases/v2.9.0.md README.md Formula/vole.rb
git commit -m "$(cat <<'EOF'
chore(release): 2.9.0 ratatui home menu (T5)

Ship mole-aligned bare-vole home navigation and document the
Clean/Optimize plan-only caveat until T6.
EOF
)"
```

---

### Task 7: 自检闸门（合入前）

- [ ] **Step 1: 对照 design §3.5 验收**

| 验收项 | 证据 |
|---|---|
| TTY 裸 `vole` 五项 + footer；无虚标未接线键 | `controls_line` 单测 + 手工 TTY |
| Enter / 数字启动正确 argv（无 `--plan`） | `HomeCommand::argv` 单测 |
| 非 TTY / 已带子命令不变 | `interactive_cli` + 既有子命令测 |
| Q / Esc / Ctrl+C 终端可恢复 | `TerminalGuard` + `HomeKey::Quit` 映射 |
| README 首页一行 + Clean/Optimize→T6 | Task 5 |
| MINOR 2.9.0；不 bump schema | Task 6 + `rg schema_version` 无协议 bump |
| 不用 PaginatedMultiSelect | `home_menu.rs` 无引用 |
| drain 进入前 | `run_home_menu` 调用 `drain_pending_input` |

- [ ] **Step 2: 全量相关测**

```bash
cargo test -p vole-cli
cargo fmt --all -- --check
# macOS 本机另可：手工 `cargo run -p vole-cli` 看首页；Q 退出后 shell 正常
```

- [ ] **Step 3: 开 PR（若执行阶段需要）**

标题建议：`feat(cli): ratatui home menu mole parity (2.9.0)`  
Body 链到 design + 本 plan；Test plan 勾选上表；注明 T6–T8 不在本 PR。

---

## Spec coverage (self-review)

| Spec 要求 | Task |
|---|---|
| ASCII Vole + URL + tagline | Task 2 |
| 五项文案对齐 mole | Task 1 |
| ↑↓ / 1–5 / Enter / M / V / T / U / Q·Esc·Ctrl+C | Task 1–2 |
| footer 条件 T/U（mole elif） | Task 1 |
| exec 式不回菜单；裸 argv | Task 1 + 3 |
| drain；TerminalGuard；Theme；非 PaginatedMultiSelect | Task 2 |
| 非 TTY exit 2 | Task 3 |
| 更新条复用 cache、裸入口不联网 | Task 4 |
| README + Clean/Optimize T6 诚实句 | Task 5 |
| MINOR 2.9.0；不 bump schema | Task 6 |
| T6–T8 不做 | Out of scope 表（无 Task） |

## Placeholder scan

无 TBD/TODO；`HomeCommand` / `HomeAction` / `HomeMenuRunOpts` 命名在 Task 1–2 定义，后续引用一致。Task 3 依赖 Task 4 的 cache API：执行时严格按 Task 1→2→4→3→5→6→7（若已按文档顺序做完 3 的占位，4 再填实亦可，但推荐 **1 → 2 → 4 → 3**）。

## 推荐执行顺序（防接口空窗）

1. Task 1 `HomeMenuState`
2. Task 2 `home_menu` 渲染
3. Task 4 update cache helpers
4. Task 3 `interactive.rs` 接线（读 cache）
5. Task 5 文档
6. Task 6 发版
7. Task 7 闸门
