diff --git a/data/rules/user-devtools.toml b/data/rules/user-devtools.toml
index e4ba6b7..dd20b03 100644
--- a/data/rules/user-devtools.toml
+++ b/data/rules/user-devtools.toml
@@ -2243,6 +2243,54 @@ last_verified = "2026-07"
 [rule.strategy]
 kind = "all"
 
+[rule.guards]
+not_running = ["Firefox"]
+
+[[rule]]
+id = "dropbox-cache"
+category = "user-devtools"
+label = "Dropbox cache"
+platform = ["macos"]
+paths = [
+  "~/Library/Caches/com.dropbox.*",
+  "~/Library/Caches/com.getdropbox.dropbox",
+]
+last_verified = "2026-07"
+
+[rule.strategy]
+kind = "all"
+
+[rule.guards]
+not_running = ["Dropbox"]
+
+[[rule]]
+id = "google-drive-cache"
+category = "user-devtools"
+label = "Google Drive cache"
+platform = ["macos"]
+paths = ["~/Library/Caches/com.google.GoogleDrive"]
+last_verified = "2026-07"
+
+[rule.strategy]
+kind = "all"
+
+[rule.guards]
+not_running = ["Google Drive"]
+
+[[rule]]
+id = "onedrive-cache"
+category = "user-devtools"
+label = "OneDrive cache"
+platform = ["macos"]
+paths = ["~/Library/Caches/com.microsoft.OneDrive"]
+last_verified = "2026-07"
+
+[rule.strategy]
+kind = "all"
+
+[rule.guards]
+not_running = ["OneDrive"]
+
 [[rule]]
 id = "firefox-profile-cache"
 category = "user-devtools"
diff --git a/docs/wukong-code/plans/reports/task-5-rules-subset.md b/docs/wukong-code/plans/reports/task-5-rules-subset.md
new file mode 100644
index 0000000..0ca3871
--- /dev/null
+++ b/docs/wukong-code/plans/reports/task-5-rules-subset.md
@@ -0,0 +1,52 @@
+# Task 5 Report: Rules data subset for not_running guards
+
+## What was implemented
+
+1. **`firefox-cache`** — added `[rule.guards] not_running = ["Firefox"]` to existing rule in `data/rules/user-devtools.toml`.
+
+2. **Three new rules** (category `user-devtools`, `platform = ["macos"]`, `kind = "all"`, `last_verified = "2026-07"`):
+   - `dropbox-cache` — paths `~/Library/Caches/com.dropbox.*`, `~/Library/Caches/com.getdropbox.dropbox`; `not_running = ["Dropbox"]`; label "Dropbox cache"
+   - `google-drive-cache` — path `~/Library/Caches/com.google.GoogleDrive`; `not_running = ["Google Drive"]`; label "Google Drive cache"
+   - `onedrive-cache` — path `~/Library/Caches/com.microsoft.OneDrive`; `not_running = ["OneDrive"]`; label "OneDrive cache"
+
+3. **Four clean fixtures** under `tests/fixtures/clean/`:
+   - `batch_guard_firefox_cache_selects_child.json` — `~/Library/Caches/Firefox/temp`
+   - `batch_guard_dropbox_cache_selects_child.json` — `~/Library/Caches/com.dropbox.foo` (+ cache file)
+   - `batch_guard_google_drive_cache_selects_child.json` — `~/Library/Caches/com.google.GoogleDrive`
+   - `batch_guard_onedrive_cache_selects_child.json` — `~/Library/Caches/com.microsoft.OneDrive`
+
+Paths/labels align with mole `lib/clean/user.sh` (`pgrep -x` names and `safe_clean` labels).
+
+## TDD Evidence
+
+Task 5 is data-only (no new Rust). Verification is fixture-driven:
+
+### GREEN
+
+```bash
+cargo test -p vole-core verify_clean_fixtures -- --nocapture
+# test clean_fixture::verify_clean_fixtures::all_extracted_fixtures_satisfy_plan_expectations ... ok
+
+cargo test -p vole-core -- --nocapture
+# test result: ok. 127 passed; 0 failed; 1 ignored
+```
+
+All four new fixtures pass with default `Orchestrator::new()` / `PgrepProcessProbe` on this machine (Firefox/Dropbox/Google Drive/OneDrive not running).
+
+## Files changed
+
+- `data/rules/user-devtools.toml` — guards on `firefox-cache`; new `dropbox-cache`, `google-drive-cache`, `onedrive-cache`
+- `tests/fixtures/clean/batch_guard_firefox_cache_selects_child.json` (new)
+- `tests/fixtures/clean/batch_guard_dropbox_cache_selects_child.json` (new)
+- `tests/fixtures/clean/batch_guard_google_drive_cache_selects_child.json` (new)
+- `tests/fixtures/clean/batch_guard_onedrive_cache_selects_child.json` (new)
+
+## Self-review
+
+- Spec coverage: all four rules + four fixtures per brief; mole-aligned paths and process names.
+- `verify_clean_fixtures` uses real `pgrep` — if Firefox/Dropbox/Google Drive/OneDrive is running locally or on CI, matching fixtures will fail (rule skipped, `expect_selected` empty). Guard logic is covered by Task 3/4 unit tests with `FakeProcessProbe`; these fixtures only assert path selection when idle.
+- No engine or schema changes in this task.
+
+## Concerns
+
+- **Flaky on busy machines:** Same as plan note — running target apps cause fixture failures. Acceptable; document for operators running full suite with apps open.
diff --git a/tests/fixtures/clean/batch_guard_dropbox_cache_selects_child.json b/tests/fixtures/clean/batch_guard_dropbox_cache_selects_child.json
new file mode 100644
index 0000000..b288b7e
--- /dev/null
+++ b/tests/fixtures/clean/batch_guard_dropbox_cache_selects_child.json
@@ -0,0 +1,18 @@
+{
+  "id": "batch_guard_dropbox_cache_selects_child",
+  "source_bats": "manual",
+  "source_test": "dropbox-cache selects com.dropbox.* child when Dropbox idle",
+  "fixture": [
+    {
+      "mkdir": "~/Library/Caches/com.dropbox.foo"
+    },
+    {
+      "write": "~/Library/Caches/com.dropbox.foo/cache.db",
+      "content": "fixture"
+    }
+  ],
+  "expect_selected": [
+    "~/Library/Caches/com.dropbox.foo|Dropbox cache"
+  ],
+  "expect_not_selected": []
+}
diff --git a/tests/fixtures/clean/batch_guard_firefox_cache_selects_child.json b/tests/fixtures/clean/batch_guard_firefox_cache_selects_child.json
new file mode 100644
index 0000000..309b2d0
--- /dev/null
+++ b/tests/fixtures/clean/batch_guard_firefox_cache_selects_child.json
@@ -0,0 +1,14 @@
+{
+  "id": "batch_guard_firefox_cache_selects_child",
+  "source_bats": "manual",
+  "source_test": "firefox-cache selects glob child when Firefox idle",
+  "fixture": [
+    {
+      "mkdir": "~/Library/Caches/Firefox/temp"
+    }
+  ],
+  "expect_selected": [
+    "~/Library/Caches/Firefox/temp|Firefox cache"
+  ],
+  "expect_not_selected": []
+}
diff --git a/tests/fixtures/clean/batch_guard_google_drive_cache_selects_child.json b/tests/fixtures/clean/batch_guard_google_drive_cache_selects_child.json
new file mode 100644
index 0000000..bc8d163
--- /dev/null
+++ b/tests/fixtures/clean/batch_guard_google_drive_cache_selects_child.json
@@ -0,0 +1,14 @@
+{
+  "id": "batch_guard_google_drive_cache_selects_child",
+  "source_bats": "manual",
+  "source_test": "google-drive-cache selects cache dir when Google Drive idle",
+  "fixture": [
+    {
+      "mkdir": "~/Library/Caches/com.google.GoogleDrive"
+    }
+  ],
+  "expect_selected": [
+    "~/Library/Caches/com.google.GoogleDrive|Google Drive cache"
+  ],
+  "expect_not_selected": []
+}
diff --git a/tests/fixtures/clean/batch_guard_onedrive_cache_selects_child.json b/tests/fixtures/clean/batch_guard_onedrive_cache_selects_child.json
new file mode 100644
index 0000000..4330c3f
--- /dev/null
+++ b/tests/fixtures/clean/batch_guard_onedrive_cache_selects_child.json
@@ -0,0 +1,14 @@
+{
+  "id": "batch_guard_onedrive_cache_selects_child",
+  "source_bats": "manual",
+  "source_test": "onedrive-cache selects cache dir when OneDrive idle",
+  "fixture": [
+    {
+      "mkdir": "~/Library/Caches/com.microsoft.OneDrive"
+    }
+  ],
+  "expect_selected": [
+    "~/Library/Caches/com.microsoft.OneDrive|OneDrive cache"
+  ],
+  "expect_not_selected": []
+}
