# Copy to scripts/signing.env (gitignored) on a Mac with the cert in Keychain.
#   cp scripts/signing.env.example scripts/signing.env
#
# Team ID: WCYC8XY4V2

export VOLE_CODESIGN_IDENTITY="Developer ID Application: Kong Wu (WCYC8XY4V2)"

# Optional notarization (pick one):
#   bash scripts/setup-notary-profile.sh
# export VOLE_NOTARY_PROFILE="vole-notary"

# CI / headless (GitHub Actions sets these; do not commit):
# export APPLE_API_KEY_BASE64="..."
# export APPLE_API_KEY_ID="AB12CD34EF"
# export APPLE_API_ISSUER_ID="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
